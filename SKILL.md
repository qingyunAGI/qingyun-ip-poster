---
name: qingyun-ip-poster
description: 将人物照片、现有账号九宫格、角色设定、活动信息或观点文案转化为高级竖版海报与连续视觉系统。用于个人 IP 账号视觉诊断与批量海报、Keynote 观点海报、圆桌嘉宾卡与活动总海报、赛博修仙/创意 IP 角色主视觉；支持 3:4 与 9:16，按任务路由生成单张、成套或 5/7/10 张差异方案，并严格控制身份一致性、事实、中文排版、系列统一与交付质检。
---

# Qingyun 高级海报 Skill

## 核心任务

把零散素材编译成信息优先、视觉有判断、可以直接传播的竖版海报。先确定海报类型，再决定素材、比例、版本数量、图像层与文字层；不要把所有需求都套进同一张黑底人物模板。

交付必须同时做到：

- 远看先看到人物、角色或活动主张，近看再读身份与细节。
- 现实人物保持身份一致；虚构角色保持设定一致。
- 姓名、职位、奖项、数字、日期、Logo 与引语均有来源，不补写事实。
- 中文、Logo 和关键日期使用可控的后期排版，不依赖生成模型碰运气。
- 单张只有一个视觉中心和一个强调色；系列共享视觉 DNA，但构图不是简单换色。
- 用户提供现有账号截图时，先审计视觉秩序，再建立可重复规则；没有数据时不把视觉变化表述为流量或转化提升。

## 先路由任务

根据用户目标只选择一个主模式；跨模式项目按“主海报 + 衍生卡”拆分。

| 模式 | 默认交付 | 典型输入 | 默认比例 |
| --- | --- | --- | --- |
| `business-ip-batch` 商务个人 IP 批量 | 账号视觉审计 + 7 个差异方向，选 1—3 张精修 | 现有九宫格（可选）+ 3—5 张同一人物照片 + 观点/履历 | 3:4 |
| `keynote-manifesto` Keynote 观点宣言 | 3 个方向或用户指定 1 张 | 演讲/半身照 + 一句可核实观点 | 3:4 |
| `event-campaign-set` 嘉宾与活动整套 | 1 张活动总海报 + N 张嘉宾卡 | 活动事实表 + Logo + 嘉宾资料/照片 | 总海报 9:16；嘉宾卡 3:4 |
| `creative-ip-keyart` 创意 IP 角色主视觉 | 高键角色档案 + 低键世界观 Key Art 双版本 | 角色设定、角色参考图或世界观简报 | 3:4 |

若用户已经给齐素材并说“直接做”，不要重复询问：

- 商务批量默认 7 张。
- Keynote 默认 3 个构图方向。
- 活动整套按实际嘉宾数生成，不强行凑 5/7/10。
- 创意 IP 默认生成白/黑双版本。

素材不完整或类型混杂时，读取 `references/intake-guide.md`，只补问会改变成品的事实。

## 建立内部信息卡

先在内部整理，不把长分析倒给用户：

- `mode`：四种主模式之一
- `deliverables`：单张、双版本、批量或活动整套
- `subject_or_character`：现实人物或虚构角色
- `main_headline`：唯一主张
- `accent_phrase`：唯一强调短语、关键词或数字
- `identity_and_credentials`：姓名、身份与最多 5 条已核实信息
- `event_facts`：活动名、日期、时间、平台、地点、主办方、CTA
- `brand_assets`：Logo、字体、品牌色、禁用项
- `references`：身份锚点图、姿态图、角色图或风格参考图
- `existing_account_grid`：现有账号/九宫格截图，以及只可观察、不应推断的边界
- `visual_dna`：字体、网格、人物尺度、色彩、标题与姓名/身份区的连续规则
- `must_keep_exact`：必须原样保留的文字
- `verification_state`：已核实 / 用户提供 / 待核实
- `ratio_and_platform`：3:4 或 9:16，以及发布平台

把示例图中的姓名、身份、奖项、日期、Logo、角色名与文案全部视为版式占位，不视为可复用事实。

## 执行流程

### Phase 0：确认边界

1. 识别模式与交付组合。
2. 检查人物/角色参考、主标题、事实与比例。
3. 商务账号若有现有九宫格，检查人物锚点、信息层级、系列一致性与缩略图可读性。
4. 标记待核实内容；没有来源就删去或保留为占位符。
5. 确认哪些文字必须原样保留。

### Phase 1：编译内容

1. 把主标题压缩成一个判断句或一个事件主题。
2. 按语义断成 2—4 行，不拆词，不留孤立标点。
3. 只选一个强调短语；姓名、活动名和强调短语不能同时抢第一视觉。
4. 删除弱履历；嘉宾卡身份最多 2 行，总海报每位嘉宾最多 2—3 行。
5. 为活动整套建立唯一事实表，所有衍生卡从同一份事实表取值。

### Phase 2：选择设计范式

读取 `references/design-system.md`，从范式库中选主样式与一个备选样式。需要查看原始样本时，读取对应的 `assets/style-references/*.png`；提取结构、层级和材质，不复制示例里的专属文案、人物身份或 Logo。

### Phase 3：拆分图像层与排版层

把生产拆成两层：

1. **图像层**：人物/角色、场景、光线、材质、留白和安全区。
2. **排版层**：准确中文、姓名、身份、日期、Logo、线框、编号和微型英文。

优先生成无字底图，再用可编辑工具完成文字。只有在当前图像工具对目标文字足够可靠且最终仍会逐字校对时，才允许直接生成文字成品。

读取 `references/prompt-compiler.md`，为每个方向写独立提示词；不要只替换强调色。

### Phase 4：生成与编排

- 现实人物：把最清晰正脸设为身份锚点，把半身/演讲图仅作为体型和姿态参考。
- 虚构角色：锁定脸、发型、服装、武器、饰件、阵营和材质，不让白版与黑版变成不同角色。
- 系列嘉宾：统一眼线高度、人物尺度、裁切、光比、标题起始线、姓名基线和页脚。
- 活动总海报：先搭建舞台与信息分区，再放准确主标题、日期、Logo、嘉宾矩阵和 CTA。
- 所有成品：保留至少 5% 横向安全边距；3:4 底部至少 7%，9:16 重要 CTA 避开平台底部操作区。

### Phase 5：质检与修复

读取 `references/quality-checklist.md`。逐张检查身份、角色一致性、事实、文字、肢体、器物、缩略图可读性、平台裁切与系列差异。

任何版本出现以下问题都先修复，不用凑数：

- 人脸或角色漂移
- 关键中文、日期、姓名或 Logo 错误
- 虚构奖项、客户、活动、品牌或现实人物身份
- 手、麦克风、剑、链条、盔甲、桌椅透视畸形
- 黑衣/黑甲与背景融成一团，或白发/银甲在白底消失
- 标题在手机缩略图中不可读
- 同批版本只有换色差异

修复时读取 `references/failure-modes.md`，优先局部重做或重排，不盲目整张重生。

## 四类模式的硬约束

### 商务个人 IP 批量

- 要求 3—5 张同一人物参考图，至少一张清晰正脸。
- 用户提供现有账号截图时，先用一句话概括当前视觉问题，再定义 `visual_dna`：字体系统、网格、人物尺度、主辅色、标题结构、姓名/身份区和页脚。
- 只分析截图能支持的视觉秩序：人物是否稳定、标题是否先读、网格是否统一、缩略图是否可辨。除非用户提供同口径数据，不推断点击、流量、转化或商业效果。
- 规划 2—3 个可连续使用的版式家族与近景/半身/环境三档人物尺度，让账号统一但不僵化。
- 使用 5/7/10 版本矩阵；每两张至少在景别、人物位置、动作、场景、标题结构中的三项不同。
- 保持商业编辑感，不做廉价课程促销、互联网运营贴纸或蓝紫科技模板。
- 先在单张尺寸验收，再把整组缩成九宫格验收；九宫格中仍应先识别人和主张。
- 需要批量规划时读取 `references/version-matrix.md`。

### Keynote 观点宣言

- 让观点先于履历；人物回答“谁在说”，标题回答“说了什么”。
- 观点优先控制在 14—24 个汉字、2—3 行，只强调 2—5 个字。
- 背景巨字只作低对比纹理，可以被人物遮挡，不承担阅读任务。
- 身份最多 2 行，不把观点卡做成人物简历。

### 嘉宾与活动整套

- 让嘉宾卡共享网格、字体比例、眼线、页脚和活动事实，只替换人物、观点和一个强调色。
- 让总海报优先表达活动主题、日期时间和多人对话场景，再承载嘉宾阵容。
- 总海报建议 4—8 位嘉宾；超出时拆成阵容分页，不无限压小字号。
- 不依据姓名生成现实人物；必须使用获得授权的照片或用户提供的明确参考。

### 创意 IP 角色主视觉

- 使用“古典秩序 × 未来器物”，不要退化为霓虹城市 + 满屏 HUD。
- 高键白版使用右偏近景、强透视武器、左侧档案留白；暗黑版使用中轴仪式构图、侧栏信息与低对比世界观暗纹。
- 把红色控制为 3%—7% 的点睛色；让银铬、枪灰、黑曜漆与克制虹彩构成材质主线。
- 让生成模型只负责角色、器物、光线与留白；角色名、编号、徽记和微型信息统一后期排版。

## 批量与系列差异

系列一致性来自：

- 同一人物/角色身份
- 同一事实表与主题
- 同一字体系统、网格、人物尺度、姓名区、页脚和材质语言
- 同一对比度与强调色强度

真实差异来自：

- 景别与人物位置
- 正面、侧面、演讲、思考或仪式动作
- 高键、低键、现场、棚拍、舞台或世界观环境
- 标题结构、信息分栏和视觉强调点

为多版本或活动整套生成矩阵时，读取 `references/version-matrix.md`。

## 交付格式

生成前只做极简确认：

> 素材已齐。我会按「{模式}」制作 {交付组合}，重点保证身份/角色一致、事实准确、标题可读和系列差异；关键中文与 Logo 采用可控排版。

生成后：

- 直接展示海报或成套结果。
- 用一句话推荐最值得精修的 1—3 个版本。
- 不重复描述图片内容，不输出冗长设计理论。
- 说明任何仍为占位或待核实的信息。

## 参考文件

- 素材不完整或需要选择模式：`references/intake-guide.md`
- 选择范式、网格、色彩和样本资产：`references/design-system.md`
- 规划 5/7/10 张或活动整套：`references/version-matrix.md`
- 编译图像层和排版层提示词：`references/prompt-compiler.md`
- 交付前验收：`references/quality-checklist.md`
- 定位并修复失败：`references/failure-modes.md`

## 硬性禁止项

- 不把示例中的现实身份、奖项、日期、Logo 或角色文案当作事实复用。
- 不把所有海报都做成同一个黑底粗字模板。
- 不在单张使用两个以上高饱和强调色。
- 不滥用蓝紫霓虹、HUD、芯片线路、玻璃拟态、发光边框、粒子和金属 3D 字。
- 不让模型生成不可核对的姓名、中文、Logo 或品牌。
- 不用同一张人物照换背景冒充多个版本。
- 不把非同题材、非同发布时间的前后截图包装成流量或转化 A/B 测试。
- 不以“已经生成足够数量”为理由交付错误文字、畸形肢体或相似版式。
